<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG-ONLY overlay: allow cleartext HTTP so a debug build can approve logins
     against a plain-http LAN test node (e.g. a heyarr dev node at
     http://192.168.x.x:7777) during device testing (docs/DEVICE-TESTING.md).
     Release builds keep the platform default (cleartext blocked). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic" />
</manifest>
