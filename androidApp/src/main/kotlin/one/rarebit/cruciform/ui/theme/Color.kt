package one.rarebit.cruciform.ui.theme

import androidx.compose.ui.graphics.Color

/** Cruciform's forest-night palette, adapted from Heyarr's shared visual language. */
object VbColors {
    // Forest black, layered panels, and quiet green dividers.
    val Background = Color(0xFF0A1410)
    val Surface = Color(0xFF111E18)
    val SurfaceElevated = Color(0xFF1A2A21)
    val SurfaceSunken = Color(0xFF08100C)
    val Outline = Color(0xFF294338)
    val OutlineSoft = Color(0xFF1D3026)

    // Text
    val TextPrimary = Color(0xFFE6EBD7)
    val TextSecondary = Color(0xFFB0BDAF)
    val TextMuted = Color(0xFF7E9182)

    // Accents
    val Mint = Color(0xFF7EE0B3)
    val MintDim = Color(0xFF285D46)
    val Fern = Color(0xFF2E7D5B)
    val FernSoft = Color(0xFFB6F0D1)
    val Amber = Color(0xFFF4B860)
    val AmberDim = Color(0xFF55401F)
    val Coral = Color(0xFFFF6B6B)
    val CoralDim = Color(0xFF4A2222)

    // Tint washes for status cards (semi-transparent over the ground)
    val MintWash = Color(0x187EE0B3)
    val AmberWash = Color(0x18F4B860)
    val FernWash = Color(0x182E7D5B)

    // On-accent text
    val OnMint = Color(0xFF041712)
    val OnFern = Color(0xFF071810)
    val OnAmber = Color(0xFF241707)
}
